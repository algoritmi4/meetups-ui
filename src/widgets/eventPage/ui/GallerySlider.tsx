import { ReactElement } from "react";

interface IGallerySliderProps {
  gallery: string[];
}

export function GallerySlider({ gallery }: IGallerySliderProps): ReactElement {
  return (
    <section className="w-full">
      <h2 className="text-[28px]">Галерея</h2>1
      
    </section>
  )
}
